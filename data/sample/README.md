# Sample data

`mini_ohlcv.csv` is a deterministic synthetic file used to demonstrate the public data schema and CSV adapter. It is not Vietnamese market data.

Regenerate it with:

```bash
python scripts/generate_sample_data.py
```
